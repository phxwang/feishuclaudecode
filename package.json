{
  "name": "claude-channel-feishu",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "bin": "./server.ts",
  "scripts": {
    "start": "bun install --no-summary && bun server.ts",
    "router": "bun install --no-summary && bun router.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@larksuiteoapi/node-sdk": "^1.5.0",
    "zod": "^3.22.0"
  }
}
